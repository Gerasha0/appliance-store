package com.epam.rd.autocode.assessment.appliances.validation;

/**
 * Validation group for entity creation operations.
 * Used to enforce constraints that should only apply when creating new entities.
 */
public interface OnCreate {
}
