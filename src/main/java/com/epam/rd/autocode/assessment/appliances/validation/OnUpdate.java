package com.epam.rd.autocode.assessment.appliances.validation;

/**
 * Validation group for entity update operations.
 * Used to enforce constraints that should only apply when updating existing entities.
 */
public interface OnUpdate {
}
